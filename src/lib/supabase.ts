// Keep the original import path used throughout the ported app while relying
// on Lovable Cloud's generated, correctly configured browser client.
export { supabase } from '@/integrations/supabase/client';
